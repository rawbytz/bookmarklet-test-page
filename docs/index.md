# Bookmarklet Test Page
- a place for testing bookmarklets
<!-- https://rawbytz.github.io/bookmarklet-test-page/ -->

### Installation: Drag the link below to your bookmarks bar:

<a href="javascript:(function numberBullets_1_7(inHerit=true,pad=false){function toastMsg(str,sec,err){WF.showMessage(&quot;&lt;b&gt;&quot;+str+&quot;&lt;/b&gt;&quot;,err);setTimeout((function(){WF.hideMessage()}),(sec||2)*1e3)}function getSeqNumber(item){const n=item.getNameInPlainText()||&quot;Home&quot;;const m=n.match(/^(\d+\.)+( )(.*)/);return m?m[0].replace(m[3],&quot;&quot;):&quot;&quot;}function getZeroPaddedSeqNumber(item){const n=item.getNameInPlainText();const m=n.match(/^(\d+ )/);return m?m[1]:&quot;&quot;}function clearSeqNumber(items){WF.editGroup((function(){items.forEach((function(item){var name=item.getName();var seqNum=pad?getZeroPaddedSeqNumber(item):getSeqNumber(item);if(seqNum){WF.setItemName(item,name.replace(seqNum,&quot;&quot;))}}))}))}if(pad){inHerit=false}const selections=WF.getSelection();const parent=WF.currentItem();const prePend=inHerit?getSeqNumber(parent).trim():&quot;&quot;;const items=selections.length===0?parent.getVisibleChildren():selections;const editableItems=items.filter(item=&gt;!item.isReadOnly());if(editableItems.length===0){return void toastMsg(&quot;No visible or editable children to number&quot;,2,true)}var editCount=0;WF.editGroup((function(){editableItems.forEach((function(node,i){var name=node.getName();var seq=pad?(i+1).toString().padStart(editableItems.length.toString().length,&quot;0&quot;)+&quot; &quot;:prePend+(i+1).toString()+&quot;. &quot;;var existingSeq=pad?getZeroPaddedSeqNumber(node):getSeqNumber(node);if(existingSeq){if(existingSeq!==seq){WF.setItemName(node,name.replace(existingSeq,seq));editCount++}}else{WF.setItemName(node,seq+name);editCount++}}))}));if(editCount===0){if(confirm(&quot;Bullets are properly numbered.\n\nPress OK to CLEAR all numbering.&quot;)){clearSeqNumber(editableItems)}}else{toastMsg(&quot;Bullets numbered&quot;)}})();">numberBullets</a>
