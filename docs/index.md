# Bookmarklet Test Page
- a place for testing bookmarklets

### Installation: Drag the link below to your bookmarks bar:

<a href="javascript:(function addingMachine_3_0(){function toastMsg(str,sec,err){WF.showMessage(str,err);setTimeout(WF.hideMessage,(sec||2)*1e3)}function applyToEachItem(functionToApply,parent){functionToApply(parent);for(let child of parent.getChildren()){applyToEachItem(functionToApply,child)}}function findMatchingItems(itemPredicate,parent){const matches=[];function addIfMatch(item){if(itemPredicate(item)){matches.push(item)}}applyToEachItem(addIfMatch,parent);return matches}function getScrubbedPlainText(item){const name=item.getName();const datesAndLinks=(new DOMParser).parseFromString(name,'text/html').querySelectorAll(&quot;a, time&quot;);let plain=item.getNameInPlainText();datesAndLinks.forEach(el=&gt;plain=plain.replace(el.innerText,&quot;&quot;));return plain}const itemIsNotBacklink=item=&gt;!item.data.isBacklinkRoot&amp;&amp;!item.data.isBacklink;const isVisible=item=&gt;WF.completedVisible()||!item.isWithinCompleted();const NUM_RGX=/(^| )([\+-]?)((\d+)|(\d{1,3})(,\d{3})+)(\.\d+)?($| )/;const nameHasNumber=item=&gt;NUM_RGX.test(getScrubbedPlainText(item));const isMatch=item=&gt;item.data.search_result&amp;&amp;item.data.search_result.matches;function getNumberString(item){const match=getScrubbedPlainText(item).match(NUM_RGX);return match?match[0]:null}const strToNum=str=&gt;parseFloat(str.replace(/,/g,&quot;&quot;));const getItemLevel=item=&gt;item.getAncestors().length-current.getAncestors().length;function createBulletObj(item,num){return{id:item.data.id,level:getItemLevel(item),num:num||strToNum(getNumberString(item))}}function getTotalAndUpdateParent(child,arr){const getParentId=b=&gt;WF.getItemById(b.id).data.pa.id;const parentId=getParentId(child);const parentIndex=arr.findIndex(b=&gt;b.id===parentId);const PARENT_EXISTS=parentIndex!==-1;const NO_WRITE=PARENT_EXISTS&amp;&amp;arr[parentIndex].write===undefined;if(!PARENT_EXISTS||NO_WRITE){const siblings=arr.filter(b=&gt;getParentId(b)===parentId);const total=siblings.reduce((total,sib)=&gt;total+sib.num,0);if(PARENT_EXISTS){if(total.toFixed(2)===arr[parentIndex].num.toFixed(2)){arr[parentIndex].write=false}else{arr[parentIndex].num=total;arr[parentIndex].write=true}}else{const parent=createBulletObj(WF.getItemById(parentId),total);parent.write=true;arr.push(parent)}}}function processLevels(max,arr){for(let i=max;i&gt;0;i--){const levels=arr.filter(b=&gt;b.level===i);levels.forEach(b=&gt;getTotalAndUpdateParent(b,arr))}}function totalToText(num){const txt=num.toFixed(2);return txt.includes(&quot;-&quot;)?txt:`+${txt}`}function nuName(item,total){const origNumStr=getNumberString(item);let name=item.getName();const newTotalStr=totalToText(total);return origNumStr?name.replace(origNumStr.trim(),newTotalStr):`${name.trimRight()} ${newTotalStr}`}function writeNewTotals(arr){const bulletsToWrite=arr.filter(b=&gt;b.write===true);WF.editGroup(()=&gt;{bulletsToWrite.forEach(b=&gt;{const item=WF.getItemById(b.id);const newName=nuName(item,b.num);WF.setItemName(item,newName)})});toastMsg(`&lt;b&gt;${bulletsToWrite.length}&lt;/b&gt; bullets updated.`,3,bulletsToWrite.length===0)}function getSearchTotalAndCreateLinks(items){const htmlEscapeTxt=str=&gt;str.replace(/&amp;/g,&quot;&amp;amp;&quot;).replace(/&gt;/g,&quot;&amp;gt;&quot;).replace(/&lt;/g,&quot;&amp;lt;&quot;).replace(/\u00A0/g,&quot; &quot;).replace(/&quot;/g,&quot;&amp;quot;&quot;);return items.map(item=&gt;{let name=htmlEscapeTxt(item.getNameInPlainText());const num=getNumberString(item);if(item.isWithinCompleted()){name=name.strike();completedTotal+=strToNum(num)}else{incompleteTotal+=strToNum(num)}const pHover=item.getParent().getNameInPlainText();return`&lt;a title=&quot;${pHover}&quot; href=&quot;${item.getUrl()}&quot;&gt;${name}&lt;/a&gt;`})}const search=WF.currentSearchQuery();const current=WF.currentItem();if(current.isMainDocumentRoot()&amp;&amp;!search)return void toastMsg(&quot;Disabled from the Home page.&quot;,3,true);if(current.isReadOnly()&amp;&amp;!search)return void toastMsg(&quot;Read-Only. Cannot write to bullet.&quot;,3,true);if(search){const itemMatches=findMatchingItems(item=&gt;isVisible(item)&amp;&amp;isMatch(item)&amp;&amp;itemIsNotBacklink(item)&amp;&amp;nameHasNumber(item),current);if(itemMatches.length===0)return void toastMsg(&quot;No matches found.&quot;,2,true);var completedTotal=0;var incompleteTotal=0;const searchList=getSearchTotalAndCreateLinks(itemMatches);const searchTotal=completedTotal+incompleteTotal;const body=`&lt;pre id=&quot;sList&quot;&gt;${searchList.join(&quot;&lt;br&gt;&quot;)}&lt;br&gt;&lt;b&gt;`;const subTotals=WF.completedVisible()?`Incomplete:\t${totalToText(incompleteTotal)}&lt;br&gt;Completed:\t${totalToText(completedTotal)}&lt;br&gt;`:&quot;&quot;;const total=`Total:\t\t${totalToText(searchTotal)}&lt;/b&gt;&lt;/pre&gt;`;const buttonStyle='&lt;style&gt;.btnX{font-size:18px;background-color:gray;border:2px solid;border-radius:20px;color:#fff;padding:5px 15px;margin-top:16px;margin-right:16px}.btnX:focus,.btnX:hover{border-color:#c4c4c4;background-color:steelblue}&lt;/style&gt;';const buttons=`&lt;button type=&quot;button&quot; class=&quot;btnX&quot; id=&quot;btn1&quot;&gt;OK&lt;/button&gt;`;WF.showAlertDialog(`${body+subTotals+total}&lt;div&gt;${buttonStyle+buttons}&lt;/div&gt;`,`Filter: ${search}`);const intervalId=setInterval((function(){let btn1=document.getElementById(&quot;btn1&quot;);if(btn1){clearInterval(intervalId);const sList=document.getElementById(&quot;sList&quot;);btn1.focus();btn1.onclick=function(){WF.hideDialog()};sList.onclick=function(){WF.hideDialog()}}}),50)}else{const itemsWithNum=findMatchingItems(item=&gt;isVisible(item)&amp;&amp;itemIsNotBacklink(item)&amp;&amp;nameHasNumber(item),current);const bullets=itemsWithNum.map(item=&gt;createBulletObj(item));const MAX_LEVEL=bullets.reduce((max,b)=&gt;Math.max(b.level,max),0);processLevels(MAX_LEVEL,bullets);writeNewTotals(bullets)}})();">addingMachine</a>
