# Bookmarklet Test Page
- a place for testing bookmarklets

### Installation: Drag the link below to your bookmarks bar:

<a href="javascript:(function multiDater_0_6(format='1',useName=true,append=true){if(format==='&quot;_format_&quot;')format=&quot;1&quot;;if(typeof useName!==&quot;boolean&quot;)useName=true;if(typeof append!==&quot;boolean&quot;)append=true;function toastMsg(str,sec,err){WF.showMessage(str,err);setTimeout(WF.hideMessage,(sec||2)*1e3)}function getUserDateFormat(date,opt){const lcid={2:&quot;sv&quot;,3:&quot;ja&quot;,4:&quot;de&quot;,5:&quot;ru&quot;,6:&quot;en-gb&quot;,7:&quot;en-us&quot;};if(opt===&quot;1&quot;)return date.toLocaleDateString(&quot;en-us&quot;,{weekday:'short',year:'numeric',month:'short',day:'numeric'});if(opt===&quot;8&quot;)return date.toLocaleDateString(&quot;sv&quot;).replace(/-/g,&quot;.&quot;);if(opt===&quot;9&quot;)return date.toLocaleDateString(&quot;ru&quot;).replace(/\./g,&quot;-&quot;);if(opt===&quot;10&quot;)return date.toLocaleDateString(&quot;en-us&quot;,{day:'2-digit',weekday:&quot;short&quot;}).toUpperCase();return date.toLocaleDateString(lcid[opt])}function updateItemTime(item,newTimeStr,inName){const content=inName?item.getName():item.getNote();const time=(new DOMParser).parseFromString(content,'text/html').querySelector(&quot;time&quot;);const escapeRegExp=str=&gt;str.replace(/[.*+\-?^${}()|[\]\\]/g,'\\$&amp;');const itemRgx=new RegExp(escapeRegExp(time.outerHTML),&quot;i&quot;);let newContent=content.replace(itemRgx,newTimeStr);newContent=newTimeStr?newContent:newContent.trim();inName?WF.setItemName(item,newContent):WF.setItemNote(item,newContent)}function addTimeToItem(item,newTimeStr){const content=useName?item.getName():item.getNote();const newContent=append?`${content.trimEnd()} ${newTimeStr} `:`${newTimeStr} ${content.trimStart()}`;useName?WF.setItemName(item,newContent):WF.setItemNote(item,newContent)}const convertOutputToDate=str=&gt;new Date(`${str} 00:00`);const hasTime=str=&gt;(new DOMParser).parseFromString(str,'text/html').querySelector(&quot;time&quot;)!==null;function convertDateToWfTime(date){const name=getUserDateFormat(date,format);return`&lt;time startYear=&quot;${date.getFullYear()}&quot; startMonth=&quot;${date.getMonth()+1}&quot; startDay=&quot;${date.getDate()}&quot;&gt;${name}&lt;/time&gt;`}function addTime(names,notes,noes,pickerOutput){if(!pickerOutput)return void toastMsg(&quot;No date entered&quot;,3,true);const newDate=convertOutputToDate(pickerOutput);const newTime=convertDateToWfTime(newDate);WF.editGroup(()=&gt;{names.forEach(item=&gt;updateItemTime(item,newTime,true));notes.forEach(item=&gt;updateItemTime(item,newTime,false));noes.forEach(item=&gt;addTimeToItem(item,newTime))})}function clearTime(names,notes){WF.editGroup(()=&gt;{names.forEach(item=&gt;updateItemTime(item,&quot;&quot;,true));notes.forEach(item=&gt;updateItemTime(item,&quot;&quot;,false))})}function resetFocus(foc,sel){blurFocusedContent();foc?WF.editItemName(foc):WF.setSelection(sel)}function showAlertDialog_Date(nameTimes,noteTimes,noTimes){const today=(new Date).toLocaleDateString(&quot;sv&quot;);const dateInput=`&lt;div&gt;&lt;input type=&quot;date&quot; id=&quot;dateBx&quot; value=&quot;${today}&quot;&gt;&lt;/div&gt;`;const b1=`&lt;button type=&quot;button&quot; class=&quot;btnX&quot; id=&quot;btn1&quot;&gt;Add Dates&lt;/button&gt;`;const b2=`&lt;button type=&quot;button&quot; class=&quot;btnX&quot; id=&quot;btn2&quot;&gt;Clear Dates&lt;/button&gt;`;const b3=`&lt;button type=&quot;button&quot; class=&quot;btnX&quot; id=&quot;btn3&quot;&gt;Cancel&lt;/button&gt;`;const dateBxStyle='#dateBx{width:50%;height:20px;display:block;margin-top:5px;border:1px solid #ccc;border-radius:4px;padding:4px}';const buttonStyle='.btnX{font-size:18px;background-color:gray;border:2px solid;border-radius:20px;color:#fff;padding:5px 15px;margin-top:16px;margin-right:16px}.btnX:focus,.btnX:hover{border-color:#c4c4c4;background-color:steelblue}';const buttons=`&lt;div&gt;${b1+b2+b3}&lt;/div&gt;`;const info=`&lt;div&gt;&lt;i&gt;${nameTimes.length+noteTimes.length+noTimes.length} items selected&lt;/i&gt;&lt;/div&gt;`;const body=`&lt;style&gt;${dateBxStyle+buttonStyle}&lt;/style&gt;${info+dateInput+buttons}`;WF.showAlertDialog(body,&quot;Enter/Pick Date:&quot;);const intervalId=setInterval((function(){let dateBx=document.getElementById(&quot;dateBx&quot;);if(dateBx){clearInterval(intervalId);let userInput;const btn1=document.getElementById(&quot;btn1&quot;);const btn2=document.getElementById(&quot;btn2&quot;);const btn3=document.getElementById(&quot;btn3&quot;);dateBx.select();btn1.onclick=()=&gt;{userInput=dateBx.value;WF.hideDialog();setTimeout(()=&gt;{addTime(nameTimes,noteTimes,noTimes,userInput);resetFocus(focus,selections)},50)};btn2.onclick=()=&gt;{WF.hideDialog();setTimeout(()=&gt;{clearTime(nameTimes,noteTimes);resetFocus(focus,selections)},50)};btn3.onclick=()=&gt;{WF.hideDialog();resetFocus(focus,selections)}}}),50)}const focus=WF.focusedItem();if(focus&amp;&amp;focus.isReadOnly())return void toastMsg(&quot;Item with focus is read-only.&quot;,3,true);const selections=focus?[focus]:WF.getSelection().filter(item=&gt;!item.isReadOnly());if(selections.length===0)return void toastMsg(&quot;No editable items are selected.&quot;,3,true);WF.setSelection([]);const names=[];const notes=[];const nos=[];selections.forEach(item=&gt;{if(hasTime(item.getName()))return void names.push(item);if(hasTime(item.getNote()))return void notes.push(item);nos.push(item)});showAlertDialog_Date(names,notes,nos)})();">multiDater</a>
